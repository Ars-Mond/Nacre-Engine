//! Reference integration (a): raw wgpu + winit.
//!
//! Scaffold — the full windowed integration is implemented in task T021. For now
//! this only confirms the example crate links against the library.

fn main() {
    println!(
        "raw-wgpu reference integration — nacre-engine v{}",
        nacre_engine::VERSION
    );
}
